var defineArticle = function(source, id) {
	var article = Object.create();
	article = {
		target : [source, id],
		settings : {
			dataType : "json",
			data : 
		}
		$("text.json", getJSONValues(source, id)) {

		}
	};
}


var changeText = function(article) {
	//get elements which we want to insert text into
	$("#h-box").load("text.json ")
	var header = document.getElementById("h-box");
	var par = document.getElementById("p-box");

	//get source text file
	var source = article.text;
	if ()

	//fill the relevant elements with text from the article
	header.innerHTML = "";
	par.innerHTML = "";
}

var 